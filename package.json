{
  "name": "jigra",
  "private": true,
  "scripts": {
    "publish": "bash scripts/prerelease.sh && lerna run build && node scripts/publish-packages.js && npm run publish-push",
    "publish-push": "bash scripts/deploy.sh"
  },
  "devDependencies": {
    "lerna": "^2.4.0"
  }
}
